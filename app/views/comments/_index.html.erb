<table>
  <% comments.each do |comment| %>
    <tr>
      <td>
        <%= simple_format comment.body %>
      </td>
      <td>
        <%= comment.created_at.strftime("%Y/%m/%d") %>
      </td>
    </tr>
  <% end %>
</table>