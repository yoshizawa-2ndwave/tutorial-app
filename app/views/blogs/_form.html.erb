<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <label for="blog_title">タイトル</label><%= form.text_field :title %>

  <label for="blog_body">本文</label><%= form.text_area :body, rows: 5 %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
